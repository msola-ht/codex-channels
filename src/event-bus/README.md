# Event Bus

本目录提供进程内有界事件传递，隔离 App Server Reader 与 Telegram 等慢速消费者。

## 文件

- `index.ts`：本模块的公开导出入口。
- `bounded-queue.ts`：支持容量限制、关键事件保护、关闭和异步消费的队列。
- `event-bus.ts`：管理订阅者及其独立队列，并隔离消费者异常。

队列过载时可以合并或丢弃非关键中间事件，但不得静默丢弃审批、错误、Item 完成或 Turn 完成事件。新增后台消费者时必须保留取消和关闭路径。

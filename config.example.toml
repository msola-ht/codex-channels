version = 1
default_workspace = "main"

[telegram]
bot_token = "你的_Bot_Token"
allowed_user_ids = [123456789]
message_format = "html"

[feishu]
# 飞书可通过 codexc setup 配置；手工填写时改为 true。
enabled = false
# app_id = "cli_0123456789abcdef"
# app_secret = "你的_飞书_App_Secret"
# allowed_open_ids = ["ou_xxx"]

# 微信只能通过 codexc setup 扫码保存凭据；确认账号后可改为 true。
# [weixin]
# enabled = false
# account_id = "不透明账号@im.bot"
# allowed_user_ids = ["不透明用户@im.wechat"]

[network]
# Telegram、飞书和微信共用；留空时依次使用标准代理环境变量和当前系统代理。
# 填写的字段优先，no_proxy 按每个请求的目标域名决定直连。
http_proxy = ""
https_proxy = ""
all_proxy = ""
no_proxy = "localhost,127.0.0.1"

[ds_proxy]
# 可选：DeepSeek 模型请求的本地回环代理，用于统计思考/生成速度。开启后 Gateway 会监听该
# 地址并把 DeepSeek App Server 的 base_url 指向它；DS 请求多一跳本机转发，Gateway
# 重启的几秒窗口内 DeepSeek 请求会短暂失败。仅支持切换模式，监听必须为回环地址。
# listen = "127.0.0.1:38473"

[codex]
binary = "codex"
socket_path = "runtime/codex-app-server.sock"
default_model = ""
sandbox = "workspace-write"

[approval]
timeout_seconds = 300

[display]
# full：完整详情；compact：单行摘要；hidden：隐藏操作过程。
operation_updates = "full"
# 是否在渠道显示 App Server 的自动任务计划更新。
plan_updates = true

[storage]
database_path = "data/gateway.sqlite3"

[logging]
level = "info"

[[workspaces]]
id = "main"
name = "Main Project"
cwd = "/absolute/path/to/project"

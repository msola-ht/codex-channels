# 从 @BotFather 获取。不要提交真实 Token。
TELEGRAM_BOT_TOKEN=

# 允许访问机器人的 Telegram 用户 ID，多个 ID 用英文逗号分隔。
TELEGRAM_ALLOWED_USER_IDS=123456789

# Codex CLI 与工作目录。
CODEX_BINARY=codex
CODEX_WORKDIR=/absolute/path/to/project

# 可选：留空时使用本机 Codex 默认模型。
CODEX_MODEL=

# 只允许 read-only 或 workspace-write。
CODEX_BRIDGE_SANDBOX=workspace-write

# Telegram 审批按钮超时（秒），范围 30-3600；超时自动拒绝。
APPROVAL_TIMEOUT_SECONDS=300

# 本地 codex-tg CLI 使用的会话。私聊通常与 Telegram 用户 ID 相同。
# 只有一个白名单用户时可留空并自动采用该用户 ID。
LOCAL_CLI_CHAT_ID=
LOCAL_SOCKET_PATH=./data/bridge.sock

DATABASE_PATH=./data/bridge.sqlite3
LOG_LEVEL=INFO

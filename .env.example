# npm CLI 默认把本文件对应配置放在 ~/.codex-connect/.env。
# 从 @BotFather 获取。不要提交真实 Token。
TELEGRAM_BOT_TOKEN=

# 允许访问机器人的 Telegram 用户 ID，多个 ID 用英文逗号分隔。
TELEGRAM_ALLOWED_USER_IDS=123456789

# 可选。设置后 Telegram 强制走该 HTTP(S) 代理；未设置时读取 HTTPS_PROXY/HTTP_PROXY。
TELEGRAM_PROXY_URL=http://127.0.0.1:7890

# Codex CLI 与允许 Telegram 选择的 Workspace。
# 也可在目标目录运行：npm --prefix /path/to/Project-skill-Codex-Connect run workspace:add
CODEX_BINARY=codex
CODEX_WORKSPACES_JSON=[{"id":"main","name":"Main Project","cwd":"/absolute/path/to/project"},{"id":"other","name":"Other Project","cwd":"/absolute/path/to/other-project"}]
CODEX_DEFAULT_WORKSPACE=main

# 独立 Codex App Server 的 Unix Socket 绝对路径。
# 启动示例：codex app-server --listen unix:///absolute/path/codex-app-server.sock
CODEX_SOCKET_PATH=/absolute/path/to/codex-app-server.sock

# 可选：留空时使用本机 Codex 默认模型。
CODEX_MODEL=

# 只允许 read-only 或 workspace-write。
CODEX_BRIDGE_SANDBOX=workspace-write

# Telegram 审批按钮超时（秒），范围 30-3600；超时自动拒绝。
APPROVAL_TIMEOUT_SECONDS=300

# 只保存 Telegram chat 与 Codex Thread 的绑定，不保存消息或会话历史。
STATE_DATABASE_PATH=./data/gateway.sqlite3

LOG_LEVEL=info

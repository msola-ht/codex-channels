# 通讯渠道验收矩阵

本表是 Telegram、飞书和微信功能验收状态的统一入口，只记录外部行为是否经过真实平台验证，
不重复各模块的实现、协议与测试细节。实现范围以根目录
[`README.md`](../README.md) 为准；模块边界与离线验证入口分别见
[`src/surfaces/telegram/README.md`](../src/surfaces/telegram/README.md)、
[`src/surfaces/feishu/README.md`](../src/surfaces/feishu/README.md) 和
[`src/surfaces/weixin/README.md`](../src/surfaces/weixin/README.md)。

## 状态定义

- **实机通过**：操作者已经在真实平台客户端观察到完整生产链路结果。
- **部分实机**：主路径已有真实结果，但组合、故障或边界场景仍需集中复验。
- **合同通过**：真实上游接口探针已经成功，但尚未验证完整生产 Surface。
- **离线通过**：源码、Mock 或本地合同测试通过，尚无真实平台结论。
- **未支持**：当前公开范围明确不提供；不能通过宽松回退伪装为支持。

合同探针只证明对应上游请求和响应形状，不自动升级为生产链路“实机通过”。真实平台验收也只
覆盖表中明确写出的主路径，不代表限流、断线、重投或超限边界已经通过。

## 能力边界

- 本表只验收 [`docs/index.md`](index.md) 当前支持矩阵已经接入的 Codex CLI/App Server 能力，
  以及明确标记的渠道运维或呈现能力；不能因为某个平台能够实现就扩展 Codex 语义。
- Codex 能力必须同时有当前锁定协议依据、受控类型、本地实现和验证。生成类型中存在但尚未接入
  的方法继续视为未支持。
- Setup、Doctor、菜单、输入状态、连接健康和平台媒体传输属于渠道能力，不产生新的 Thread、
  Turn、Item、工具、审批或历史事实。
- 不实现假流式、平行会话状态、平台私有工具或静默扩大的审批语义。平台无法准确承载某项能力时，
  使用受约束降级或明确标记为未支持。

## 当前结论

截至 2026-07-30，三个渠道的授权私聊、Codex Turn 主路径、基础命令、引用、命令审批和官方
Plan 用户输入已经可以使用。其他审批种类以表中注明的离线或实机范围为准。下一阶段以真实平台
集中验收和故障边界为主，不继续为已覆盖行为增加新的共享抽象。

| 能力 | Telegram | 飞书 | 微信 |
| --- | --- | --- | --- |
| 授权私聊文本、最终回复 | 实机通过 | 实机通过 | 实机通过 |
| 基础命令、状态、上线与 Turn 统计 | 实机通过 | 实机通过 | 实机通过 |
| 平台回复/引用转有界上下文 | 实机通过 | 实机通过 | 实机通过 |
| Turn 开始确认与任务结束顺序 | 实机通过 | 实机通过 | 实机通过 |
| 命令审批主路径 | 实机通过 | 实机通过 | 实机通过 |
| 文件、权限与网络审批的协议映射与平台主边界 | 离线通过 | 离线通过 | 离线通过 |
| 官方 Plan 模式与非敏感用户输入 | 实机通过 | 实机通过 | 实机通过 |
| 工作状态展示 | 实机通过：原生 Typing | 实机通过：状态卡与原生流式卡 | 实机通过：原生输入状态 |
| 持续回复展示 | 实机通过：平台消息流 | 实机通过：CardKit 原生流式主路径 | 按设计使用块式最终回复，不模拟原生流式 |
| 单张 PNG/JPEG 输入 | 实机通过 | 实机通过 | 实机通过 |
| 多图与说明文字同次输入 | 实机通过：原生相册 | 实机通过：两至四图加文字的顺序和单 Turn | 部分实机：图片收取通过，连续图片合并为单次 Turn 待集中复验 |
| UTF-8 文本文件输入 | 实机通过 | 离线通过 | 实机通过 |
| 一次性语音输入 | 平台接收与离线转换通过；当前模型目录无 `audio`，Turn 前拒绝 | 实机接收与协议转换通过；当前模型目录无 `audio`，Turn 前拒绝 | 可信转写可用；原始 MP3/OGG 当前因模型目录无 `audio` 在 Turn 前拒绝，SILK 未支持 |
| `imageGeneration` 图片回传 | 离线通过 | 实机通过 | 实机通过 |
| 超长最终回复文件兜底 | 离线通过 | 离线通过 | 离线通过 |
| MCP form/URL elicitation | 离线通过 | 离线通过 | 离线通过 |
| Gateway 重启后的会话恢复 | 实机通过 | 实机通过 | 实机通过 |
| 限流、断线、重投与失败降级 | 部分实机，故障矩阵待集中复验 | 离线通过，待集中实机验收 | 部分实机：轮询恢复主路径通过，故障矩阵待集中复验 |
| HTTP/HTTPS 代理 | 离线通过 | 离线通过 | 离线通过 |
| 群聊 | 未支持 | 未支持 | 未支持 |
| 实时语音、语音输出、视频、Office、压缩包等输入 | 未支持 | 未支持 | 未支持 |

## 下一轮集中验收

按以下顺序执行；每一项完成后先更新本表和对应模块记录，再进入下一项。

1. 飞书 UTF-8 文本文件输入与超长最终回复文件，分别验证文件名、正文和预览/完整文件顺序。
2. 飞书 MCP form 与 URL 卡片，验证一次性令牌、Actor/Chat/消息绑定、取消和跨客户端失效。
3. 飞书限流、流式失败回退、超长滚动、断线恢复、未授权/重复事件和代理。
4. 微信连续多图合并、超长最终回复文件、MCP form/URL 以及轮询故障恢复。
5. Telegram `imageGeneration`、超长最终回复文件和限流/断线集中复验。

## 记录规则

- 只记录脱敏的日期、渠道、能力、结果和失败分类，不保存消息正文、账号、Chat、Token、链接或
  上游完整响应。
- “实机通过”必须由操作者在对应客户端明确确认；仅看到探针成功不能替代生产 Surface 验收。
- 修复后必须重新验证原失败场景；无关既有实机结论不机械重跑。
- 当前未支持能力继续失败关闭，不因为其他渠道支持就增加隐式兼容或宽松解析。
